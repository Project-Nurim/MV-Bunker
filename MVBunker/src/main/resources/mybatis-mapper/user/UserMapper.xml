<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nurim.mvbunker.user.UserMapper">
    <insert id="insUser" useGeneratedKeys="true" >
        INSERT INTO t_user
        (uid, ex_key, provider, upw, unm, age, unn, profileImg, auth)
        VALUES
        (#{uid}, #{ex_key}, #{provider}, #{upw}, #{unm}, ${age}, #{unn}, #{profileImg}, #{auth})
    </insert>
    <update id="authUser">
        UPDATE t_user
        SET auth = null
        WHERE uid = #{uid} AND auth = #{auth}
    </update>
    <select id="selUser" resultType="UserEntity">
        SELECT *
        FROM t_user
        WHERE uid = #{uid} AND auth IS NULL AND provider = #{provider}
    </select>
    <delete id="delUser">
        delete from t_user
        where uid = #{uid} and upw = #{upw}
    </delete>

    <update id="updUser">
        <set>
            <if test="profileImg != null and profileImg != ''">
                profileImg = #{profileImg}
            </if>
            <if test="upw != null and upw != ''">
                upw = #{upw}
            </if>
            <if test="unn != null and unn != ''">
                unn = #{unn}
            </if>
            <if test="introduce != null and introduce != ''">
                introduce = #{introduce}
            </if>
        </set>
    </update>


    <!-- 내가 작성한 리뷰 수 카운트 -->
    <select id="countMyReview"  resultType="int">
        select count(*)
        from t_review
        where i_user = ${i_user};
    </select>

    <!-- 내가 작성한 댓글 수 카운트 -->
    <select id="countMyCmt" resultType="int">
        select count(*)
        from t_review_cmt
        WHERE i_user = ${i_user}
    </select>

    <!-- 내가 작성한 답글 수 카운트 -->
    <select id="countMyReply" resultType="int">
        select count(*)
        from t_reply
        where i_reply_user = ${i_user}
    </select>

    <!-- 내가 팔로우한 유저목록 띄우기 -->
    <select id="mySubUser">
        select * from t_sub
        where sub_ing_user = ${i_user}
    </select>
</mapper>